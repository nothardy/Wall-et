const initialState = {
    count: [],
    cokiees: {}
}

function rootReducers(state = initialState, action){

    switch (action.type) {

        default:
            return state
    }
}
export default rootReducers;
